import React from "react";
import {motion,useAnimation} from "framer-motion";
import "./style.scss";

const SidebarDropdown = props => {
    return (
    <motion.div initial={{scale:"1"}} className="SidebarDropdown__container">
    </motion.div>);
}

export default SidebarDropdown;